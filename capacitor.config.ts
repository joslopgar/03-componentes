import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.lopgar.firstapp',
  appName: 'componentes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
